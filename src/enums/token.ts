export enum Token {
  Keep = "KEEP",
  Nu = "NU",
  T = "T",
}

export enum CoingeckoID {
  KEEP = "keep-network",
  NU = "nucypher",
  T = "ttoken",
}

export enum TConversionRates {
  KEEP = 4.87,
  NU = 3.46,
}
