import { FC } from "react"
import { Button, ModalBody, ModalFooter } from "@threshold-network/components"

const AnalyticsAccepted: FC = () => {
  return (
    <>
      <ModalBody>Accept</ModalBody>
      <ModalFooter>
        <Button>Accept</Button>
      </ModalFooter>
    </>
  )
}

export default AnalyticsAccepted
