module.exports = {
  StarknetConfig: jest.fn(),
  useAccount: jest.fn(),
  useProvider: jest.fn(),
  useConnect: jest.fn(),
  useDisconnect: jest.fn(),
  useNetwork: jest.fn(),
  useStarknet: jest.fn(),
}
